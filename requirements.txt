# Core ML / Embeddings (fine-tune + benchmark)
torch>=2.0.0
numpy<2.0
sentence-transformers==3.3.1
transformers
accelerate
datasets

# RAGAS evaluation
ragas==0.2.14
langchain-google-genai
langchain-community==0.3.27
langchain-core==0.3.65

# PDF (data generation / instruction data)
PyMuPDF

# Utilities
python-dotenv
